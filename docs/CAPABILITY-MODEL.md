# Capability model

See [capability-permission.md](./capability-permission.md). The firewall is the authorization engine. Skill text and manifests cannot add effective permissions. `request_capability` is audited and may `DENY` or `REQUIRE_USER_APPROVAL`.
