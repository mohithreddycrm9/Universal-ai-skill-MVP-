# Threat model

See [threat-model.md](./threat-model.md). Gateway-specific: prompt injection in SKILL.md, rug-pulls via floating branches, cache poisoning (mitigated by fingerprinting security config + files), and agents skipping the gate (mitigated by quarantine-first lifecycle + illegal transition checks).
